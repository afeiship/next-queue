(function() {
  const NxQueue = require('../src');

  describe('NxQueue.methods', function() {
    test('init', function() {
      const data = { key: 1, value: 2 };
      expect(!!data).toBe(true);
    });
  });
})();
